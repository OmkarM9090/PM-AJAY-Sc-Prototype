# Voice Rules

- Inbound phone call is the primary telephony demo.
- Never block the product if microphone/provider is unavailable.
- Show clear states: listening, processing, understood, retry.
- Language must remain consistent during a call unless user changes it.
- Persist partial profile after every successful turn.
